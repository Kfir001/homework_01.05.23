function multiplication(a, b) { //? הגדרת פונקציה עם שני ארגיומנטים
    return a * b; //? הפונקציה מחזירה את התוצאה של הכפלת שני הארגיומנטים
  }
  console.log(multiplication(5, 7)); //? הדפסה של הפונקציה בקונסול